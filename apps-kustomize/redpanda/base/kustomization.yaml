apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Order matters for apply: PVC must exist before Deployment (which mounts it)
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - nodeport.yaml
