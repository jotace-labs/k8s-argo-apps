apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

namePrefix: dev-

resources:
  - ../../base

patches:
  - path: deployment.yaml
    target:
      kind: Deployment
      name: my-consumer # should match the metadata.name
