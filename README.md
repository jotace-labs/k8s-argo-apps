# Manifestos handler

todo:

- create actions in each app to:
  - build docker image
  - log into registry
  - push into registry using some identifier (tag or sha)

install argo chart in cluster to manage all apps using this repo

configure deployment workflows to commit to this repo and make argo point here
